# Optional: enables free-form chatbot answers via OpenAI.
# Copy to .env.local (gitignored). Without it the chatbot runs in demo mode.
# NOTE: a VITE_ variable is embedded in the built JS — never use a real key
# in a public deployment; paste the key into the chat panel instead.
VITE_OPENAI_API_KEY=
